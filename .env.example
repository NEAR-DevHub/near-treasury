# FastNEAR API Key (optional)
# Get your API key from https://fastnear.com
# This helps avoid rate limits when making RPC calls to the NEAR blockchain
#
# Note: This uses NEXT_PUBLIC_ prefix, which means the API key will be
# visible in the browser's JavaScript bundle. FastNEAR API keys are meant
# for this use case and are rate-limited per key.
NEXT_PUBLIC_FASTNEAR_API_KEY=
